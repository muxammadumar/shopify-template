<section id="profit" class="ios-section" aria-label="Beispiel‑Marge">
  <div class="ios-wrap">
    {% render 'ios-section-header', title: section.settings.title, subtitle: section.settings.subtitle %}

    <div class="ios-profit">
      <div class="ios-glass ios-padding-card">
        <h3 class="ios-text-feature-title" style="margin-bottom:6px">Szenario „Erster Verkauf"</h3>
        <p class="ios-text-feature-body">
          Der Kunde bezahlt, der Lieferant versendet automatisch. Du fokussierst dich auf Marketing und Wachstum.
        </p>

        <div class="ios-calc" aria-label="Berechnung">
          <div class="ios-row"><span>{{ section.settings.sell_label }}</span><b>{{ section.settings.sell_value }}</b></div>
          <div class="ios-row"><span>{{ section.settings.cost_label }}</span><b>{{ section.settings.cost_value }}</b></div>
          <div class="ios-row total"><span>{{ section.settings.profit_label }}</span><b>{{ section.settings.profit_value }}</b></div>
        </div>
      </div>

      <div class="ios-card ios-padding-card">
        <h3 class="ios-text-feature-title" style="margin-bottom:6px">Was danach passiert</h3>
        <p class="ios-text-feature-body">
          Nach dem Versand erhält der Kunde Updates und Tracking. Bei Fragen wendet er sich an deinen Brand‑Store.
        </p>
        <ul class="ios-list">
          <li>Versand beim Lieferanten – ohne deine Beteiligung</li>
          <li>Automatische Benachrichtigungen/Status</li>
          <li>Du konzentrierst dich auf Werbung und Conversion</li>
        </ul>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "iOS Profit",
  "settings": [
    { "type": "text", "id": "title", "label": "Title", "default": "Beispiel‑Marge (Modell)" },
    { "type": "textarea", "id": "subtitle", "label": "Subtitle", "default": "Typisches Modell: Du verkaufst teurer, der Lieferant liefert günstiger." },

    { "type": "text", "id": "sell_label", "label": "Sell label", "default": "Verkaufspreis" },
    { "type": "text", "id": "sell_value", "label": "Sell value", "default": "39,99 €" },

    { "type": "text", "id": "cost_label", "label": "Cost label", "default": "Lieferantenpreis (inkl. Versand)" },
    { "type": "text", "id": "cost_value", "label": "Cost value", "default": "9,99 €" },

    { "type": "text", "id": "profit_label", "label": "Profit label", "default": "Brutto‑Marge (Beispiel)" },
    { "type": "text", "id": "profit_value", "label": "Profit value", "default": "≈ 30 €" }
  ],
  "presets": [{ "name": "iOS Profit" }]
}
{% endschema %}

