<section id="what" class="ios-section" aria-label="What's Included">
  <div class="ios-wrap">
    {% render 'ios-section-header', title: section.settings.title, subtitle: section.settings.subtitle %}

    <div class="ios-grid2">
      {% for block in section.blocks %}
        {% render 'ios-card', title: block.settings.title, text: block.settings.text, badge: block.settings.badge, attributes: block.shopify_attributes %}
      {% endfor %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "iOS Included",
  "settings": [
    { "type": "text", "id": "title", "label": "Title", "default": "What's Included" },
    { "type": "textarea", "id": "subtitle", "label": "Subtitle", "default": "Shop, packaging and basic marketing assets – all in one." }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Item",
      "settings": [
        { "type": "text", "id": "title", "label": "Title", "default": "Shopify‑Store" },
        { "type": "textarea", "id": "text", "label": "Text", "default": "Responsive design, conversion structure and handover of access credentials." },
        { "type": "text", "id": "badge", "label": "Badge", "default": "Store" }
      ]
    }
  ],
  "max_blocks": 8,
  "presets": [{ "name": "iOS Included" }]
}
{% endschema %}

