<section id="about" class="ios-section" aria-label="About Us">
  <div class="ios-wrap">
    {% render 'ios-section-header', title: section.settings.title, subtitle: section.settings.subtitle %}

    <div class="ios-about-content">
      {% if section.settings.content != blank %}
      <div class="ios-glass ios-padding-card">
        <div class="ios-about-text">
          {{ section.settings.content }}
        </div>
      </div>
      {% endif %}

      {% if section.blocks.size > 0 %}
      <div class="ios-grid2" style="margin-top: 14px;">
        {% for block in section.blocks %}
          <div class="ios-card ios-padding-card" {{ block.shopify_attributes }}>
            {% if block.settings.title != blank %}
            <h3 class="ios-text-feature-title">{{ block.settings.title }}</h3>
            {% endif %}
            {% if block.settings.text != blank %}
            <p class="ios-text-feature-body">{{ block.settings.text }}</p>
            {% endif %}
            {% if block.settings.image != blank %}
            <div class="ios-about-image" style="margin-top: 12px;">
              <img src="{{ block.settings.image | img_url: '600x400' }}" 
                   alt="{{ block.settings.image.alt | default: block.settings.title | escape }}"
                   loading="lazy"
                   style="width: 100%; border-radius: var(--radius);">
            </div>
            {% endif %}
          </div>
        {% endfor %}
      </div>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "iOS About",
  "settings": [
    { "type": "text", "id": "title", "label": "Title", "default": "About Us" },
    { "type": "textarea", "id": "subtitle", "label": "Subtitle", "default": "Who we are and what drives us." },
    { "type": "richtext", "id": "content", "label": "Content", "default": "<p>We are a team of experts specializing in launching dropshipping brands quickly and successfully.</p>" }
  ],
  "blocks": [
    {
      "type": "about_item",
      "name": "About Item",
      "settings": [
        { "type": "text", "id": "title", "label": "Title", "default": "Our Mission" },
        { "type": "textarea", "id": "text", "label": "Text", "default": "We help entrepreneurs quickly enter e-commerce without technical barriers." },
        { "type": "image_picker", "id": "image", "label": "Image", "info": "Optional image for this item" }
      ]
    }
  ],
  "max_blocks": 4,
  "presets": [{ "name": "iOS About" }]
}
{% endschema %}
